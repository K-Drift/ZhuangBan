本目录下放 **妆练 2.0** 切片视频，文件名需与代码一致：

- `妆练2.0-1.mp4` … `妆练2.0-12.mp4`（与 12 个跟练步骤一一对应）

在 `makeup-vue` 根目录执行 `npm run dev`，浏览器访问开发地址即可通过路径 `/妆练2.0/妆练2.0-{n}.mp4` 播放。

文件名若调整，同步修改 `src/data/steps.ts` 中的 `clip()` / `videosBase` 映射即可。
